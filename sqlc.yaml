version: "2"
sql:
  - engine: "postgresql"
    queries: "internal/repository"
    schema: "migrations"
    gen:
      go:
        package: "db"
        out: "internal/db/sqlc"
        emit_json_tags: true
        emit_interface: true
